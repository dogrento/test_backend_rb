<h1>Search Results</h1>

<% if @results.present? %>
  <ul>
    <% @results.each do |result| %>
      <li>
        <strong>ID:</strong> <%= result.id %><br>
        <strong>City:</strong> <%= result.city %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No cities found for the search query.</p>
<% end %>